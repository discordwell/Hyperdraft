[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    hs_unhappy: Hearthstone unhappy-path edge-case tests
    slow: tests that take nontrivial time (may be skipped in fast TDD loop)
