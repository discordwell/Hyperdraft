/**
 * Vitest setup file — runs before each test file.
 *
 * Adds @testing-library/jest-dom matchers (toBeInTheDocument, toHaveClass,
 * etc.) so component assertions read like Jest tests.
 */

import '@testing-library/jest-dom/vitest';
