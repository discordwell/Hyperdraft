"""Cats — trick-taking + pile-building card game card definitions.

This package will hold card definitions, decks, and Commander Cat
catalogues for the Cats engine. Engine code lives in
``src/engine/cats.py``. See ``docs/games/cats.md`` for the design.

Stage 4 (set-design) will populate this package; Stages 1-3 only
require the package marker so the engine module can be imported
without breaking ``src/cards/__init__.py`` discovery.
"""
